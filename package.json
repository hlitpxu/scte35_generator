{
  "name": "scte35_generator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "vue": "npm --prefix client run serve",
    "deploy": "npm --prefix client run build && npm --prefix server run start ",
    "setup": "npm install && npm --prefix client install && npm --prefix server install"
  },
  "eslintConfig": {
    "root": true,
    "env": {
      "node": true
    },
    "extends": [
      "plugin:vue/vue3-essential",
      "eslint:recommended"
    ],
    "parserOptions": {
      "parser": "@babel/eslint-parser"
    },
    "rules": {}
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead",
    "not ie 11"
  ]
}
